# 🧠 Data Science & Machine Learning Portfolio

A complete, modular collection of **7 end-to-end ML projects** — each in its own folder with a premium Streamlit web app frontend. Built with Python, Scikit-Learn, and Streamlit.

---

## 📁 Project Directory

| # | Folder | Project | Algorithm | Dataset |
|---|--------|---------|-----------|---------|
| 1 | `linear_regression_app/` | 🏠 House Price Predictor | Linear / Lasso / Ridge Regression | California Housing |
| 2 | `logistic_regression_app/` | 🔬 Tumor Classifier | Logistic Regression | Breast Cancer Wisconsin |
| 3 | `decision_tree_app/` | 🌸 Iris Flower Classifier | Decision Tree | Iris |
| 4 | `naive_bayes_app/` | 📰 News Article Classifier | Multinomial Naive Bayes + TF-IDF | 20 Newsgroups |
| 5 | `kmeans_clustering_app/` | 🔵 Clustering Explorer | K-Means + DBSCAN | Synthetic Blobs |
| 6 | `Random_Forest_Classifier/` | 🍷 Wine Quality Predictor | Random Forest | Wine Quality |
| 7 | `svm_digits_app/` | ✍️ Digit Recognizer | SVM (Linear/RBF) | Sklearn Digits |

---

## 🚀 Running Any Project

```bash
cd <project_folder>
pip install -r requirements.txt
streamlit run app.py
```

### 🏠 House Price Predictor
```bash
cd linear_regression_app && pip install -r requirements.txt && streamlit run app.py
```

### � Tumor Classifier
```bash
cd logistic_regression_app && pip install -r requirements.txt && streamlit run app.py
```

### 🌸 Iris Flower Classifier
```bash
cd decision_tree_app && pip install -r requirements.txt && streamlit run app.py
```

### 📰 News Article Classifier
```bash
cd naive_bayes_app && pip install -r requirements.txt && streamlit run app.py
```

### 🔵 Clustering Explorer
```bash
cd kmeans_clustering_app && pip install -r requirements.txt && streamlit run app.py
```

### 🍷 Wine Quality Predictor
```bash
cd Random_Forest_Classifier && pip install -r requirements.txt && streamlit run app.py
```

### ✍️ Digit Recognizer
```bash
cd svm_digits_app && pip install -r requirements.txt && streamlit run app.py
```

---

## 🎨 UI Features (All Apps)

- 🎨 Premium dark themed UI with Google Poppins font
- ✨ Glassmorphism cards and gradient effects
- 📊 Real-time predictions with animated result cards
- 📈 Confidence bars / probability charts
- 🔧 Interactive sidebar controls (model settings, hyperparameters)
- 📋 Feature importance / cluster summaries

---

## 🤖 ML Topics Covered

### Supervised Learning
| Category | Algorithms |
|---|---|
| **Regression** | Linear, Lasso (L1), Ridge (L2) |
| **Classification** | Logistic Regression, Decision Tree, Random Forest, SVM, Naive Bayes |

### Unsupervised Learning
| Category | Algorithms |
|---|---|
| **Clustering** | K-Means, DBSCAN |

### ML Engineering
- Train/Test Split, Cross-Validation (K-Fold)
- Regularization (L1/L2), Hyperparameter tuning
- Feature Scaling (StandardScaler)
- TF-IDF Vectorization for NLP
- Model evaluation metrics (Accuracy, R², RMSE, ROC-AUC)
- Streamlit Deployment

---

## 🧪 Tech Stack

`Python` &nbsp; `NumPy` &nbsp; `Pandas` &nbsp; `Scikit-Learn` &nbsp; `Streamlit` &nbsp; `Matplotlib` &nbsp; `Joblib`

---

## 📌 Upcoming Additions

- [ ] XGBoost Advanced Tuning
- [ ] Deep Learning (TensorFlow/Keras)
- [ ] NLP Applications (BERT)
- [ ] Model Monitoring Dashboard
- [ ] Docker Deployment
- [ ] CI/CD for ML Apps

---

*Built with ❤️ as part of a Data Science & ML specialization journey.*
